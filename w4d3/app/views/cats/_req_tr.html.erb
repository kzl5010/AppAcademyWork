<tr>
  <td><%= req.user.user_name %></td>
  <td><%= req.start_date %></td>
  <td><%= req.end_date %></td>
  <td>
    <% if !current_user.nil? %>
      <% if req.pending? && req.cat.user_id == current_user.id %>
          <%= button_to "Approve", approve_cat_rental_request_url(req) %>
          <%= button_to "Deny", deny_cat_rental_request_url(req) %>
      <% else %>
        <%= req.status %>
      <% end %>
    <% else %>
      <%= req.status %>
    <% end %>
  </td>
</tr>
